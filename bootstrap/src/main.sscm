#| S-lang Entrypoint.                      |
 |                                         |
 | Compile Scheme code into a Wasm module. |#
#!sweet

import (scheme base)

add-to-load-path (dirname (current-filename))  # NOT-R7RS
import (parse bindings)

display(root-bindings)
